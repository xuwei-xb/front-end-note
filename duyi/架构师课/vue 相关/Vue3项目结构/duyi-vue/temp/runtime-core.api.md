## API Report File for "@vue/runtime-core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// Warning: (ae-forgotten-export) The symbol "ComputedGetter" needs to be exported by the entry point index.d.ts
// Warning: (ae-forgotten-export) The symbol "ComputedRef" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function computed<T>(getter: ComputedGetter<T>): ComputedRef<T>

// Warning: (ae-forgotten-export) The symbol "WriteableComputedOptions" needs to be exported by the entry point index.d.ts
// Warning: (ae-forgotten-export) The symbol "WriteableComputedRef" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function computed<T>(options: WriteableComputedOptions<T>):WriteableComputedRef<T>

// @public (undocumented)
export function computed<T>(getterOrOptions: ComputedGetter<T> | WriteableComputedOptions<T>) {
    let // (undocumented)
    getter;
    let // (undocumented)
    setter;

    if (isFunction(getterOrOptions)) {
        getter = getterOrOptions;
        setter = NOOP
    }
    else {
        getter = getterOrOptions.get;
        setter = getterOrOptions.set;
    }

    return new ComputedRefImpl(
    getter,
    setter,
    isFunction(getterOrOptions) || !getterOrOptions.set
    )
}

// Warning: (ae-forgotten-export) The symbol "RendererOptions" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function createRenderer<HostNode = any, HostElement = any>(options: RendererOptions<HostNode, HostElement>): {
    render: (vnode: any, container: any) => void;
};

// Warning: (ae-forgotten-export) The symbol "ReactiveEffectOptions" needs to be exported by the entry point index.d.ts
// Warning: (ae-forgotten-export) The symbol "ReactiveEffect" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function effect<T = any>(
fn: () => T,
options: ReactiveEffectOptions = {}
): ReactiveEffect<T> {
    // 如果fn是一个副作用函数，则直接取其raw属性
    if (isEffect(fn)) {
        fn = fn.raw;
    }
    // 创建一个副作用函数
    const // (undocumented)
    effect = createReactiveEffect(fn, options);

    // 只有非lazy的情况，才会立即执行副作用函数
    if (!options.lazy) {
        effect();
    }

    // 将副作用函数作为返回值返回
    return effect;
}

// @public (undocumented)
export function h(type: any, props: any, children: any): {
    __v_isVnode: boolean;
    type: any;
    props: any;
    children: any;
    component: null;
    el: null;
    key: any;
    shapeFlags: number;
};

// Warning: (ae-forgotten-export) The symbol "Ref" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function isRef<T>(r: Ref<T> | unknown): r is Ref<T>

// @public (undocumented)
export function isRef(r: any): r is Ref {
    return Boolean(r && r.__v_isRef === true);
}

// @public (undocumented)
export function lis(arr: any): number[];

// @public (undocumented)
export function normalizeClass(value: unknown): string;

// @public (undocumented)
export const options: RendererOptions;

// @public (undocumented)
export function reactive<T extends object>(target: T): T;

// Warning: (ae-forgotten-export) The symbol "Target" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function reactive(target: object) {

    if (target && (target as Target)[ReactiveFlags.IS_READONLY]) {
        return target;
    }

    return createReactiveObject(target, false, mutableHandlers);
}

// @public (undocumented)
export function ref(value?: any): any {
    return createRef(value);
}

// @public (undocumented)
export function shouldSetAsProps(el: any, key: string, value: any): boolean;

// Warning: (ae-forgotten-export) The symbol "WatchOptions" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export function watch(source: any, cb: any, options: WatchOptions): void;

// (No @packageDocumentation comment for this package)

```
